{
  "name": "@tsenako/product-db",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --skip-generate",
    "db:deploy": "prisma migrate deploy"
  },
  "devDependencies": {
    "@tsenako/typescript-config": "workspace:*",
    "@types/node": "^22.15.3",
    "prisma": "^6.19.0"
  },
  "dependencies": {
    "@prisma/client": "^6.16.0"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
